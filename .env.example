# ============================================
# LiteLLM UI 登入憑證
# ============================================
UI_USERNAME='admin'
UI_PASSWORD='your_secure_password_here'

# ============================================
# PostgreSQL 資料庫設定
# ============================================
# 注意：這些環境變數會被 docker-compose.yml 使用
POSTGRES_DB='litellm'
POSTGRES_USER='llmproxy'
POSTGRES_PASSWORD='your_secure_db_password_here'

# 如果需要直接連接資料庫（非 Docker 環境），使用此設定
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}"

# ============================================
# LiteLLM API 金鑰
# ============================================
# 主要 API 金鑰（用於訪問 LiteLLM proxy）
LITELLM_MASTER_KEY='sk-your-master-key-here'

# OpenRouter API 金鑰（必要）
OPENROUTER_API_KEY='your_openrouter_api_key_here'

# ============================================
# 選用：其他 LLM 提供商 API 金鑰
# ============================================
# 如果使用 LM Studio 本地模型
# LM_STUDIO_API_KEY='your_lm_studio_api_key'
# LM_STUDIO_API_BASE='http://localhost:1234/v1'

# 如果使用 Ollama 本地模型
# OLLAMA_API_KEY='your_ollama_api_key'
# OLLAMA_API_BASE='http://localhost:11434'

# ============================================
# MLflow 設定（Docker 環境自動配置）
# ============================================
# 注意：使用 Docker Compose 時，以下設定會自動配置，無需手動設定
# MLFLOW_TRACKING_URI 會自動設為 http://mlflow:5000
# 如果需要覆蓋，可以取消註解以下設定：
# MLFLOW_TRACKING_URI='http://localhost:5001'
# MLFLOW_EXPERIMENT_NAME='litellm-proxy'
